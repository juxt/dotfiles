" By default == has a maximum number of lines to prevent
" hanging. Disable that, assume people are adults.
let g:clojure_maxlines = 0

" Disable fireplace's auto-nashorn
let g:fireplace_cljs_repl = ''
